<%= bootstrap_form_tag url: url do |f| %>
  <%= f.hidden_field :type, value: :saved %>
  <%= f.collection_select :product, LiftType.all, :id, :name, label: 'Filtry zapisane:' %>
  <%= f.submit 'Zastosuj', class: 'btn btn-primary' %>
<% end %>